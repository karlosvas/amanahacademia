name: SonarCloud Analysis

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  sonar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Frontend - install & test
        working-directory: ./frontend
        run: |
          npm ci --legacy-peer-deps --ignore-scripts
          npm run test:coverage

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            backend/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('backend/Cargo.lock') }}

      # Instalación mucho más rápida mediante binario precompilado
      - name: Install llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Backend - test & coverage
        working-directory: ./backend
        # Corregido: se usa --ignore-filename-regex en lugar de --exclude-files
        run: |
          mkdir -p coverage
          cargo llvm-cov --cobertura --output-path coverage/cobertura.xml --ignore-filename-regex "src/test.rs"

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v3.1.0
        with:
          projectBaseDir: .
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
