---
import Carrousel from "./Carrousel.astro";

const { id } = Astro.props;
---

<dialog
  id={id}
  class="modal border-0 rounded-2xl shadow-2xl max-w-2xl w-[95%] mx-auto p-0 overflow-hidden bg-background] p-8 bg-brown dark:bg-background"
>
  <Carrousel id={id} />
</dialog>
