---
import Layout from "@/layouts/Layout.astro";
import App from "@/layouts/App.astro";
import { getI18N } from "@/../i18n/index";

// Obtenemos el idioma obtenido mediante la ruta
const { lang } = Astro.params;
---

<Layout>
  <App data={getI18N({ lang })} />
</Layout>
